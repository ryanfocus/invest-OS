# ─────────────────────────────────────────────────────────────
# 機密 — 這裡面是可以直接下單的期貨帳密，外洩等於帳戶被接管
# ─────────────────────────────────────────────────────────────
.env
.env.local
.env.*.local
!.env.example          # 範本必須進版控，別被上面的規則掃到

# 群益下單憑證與金鑰（正常裝在系統憑證區，但以防有人複製進專案）
*.pfx
*.p12
*.cer
*.crt
*.key

# ─────────────────────────────────────────────────────────────
# 執行期產物 — 每台機器各自維護，不同步
# ─────────────────────────────────────────────────────────────
# 部位狀態。這是 13:40 出場的唯一依據，兩台機器共用會災難性地互相覆蓋
#
# ⚠️ state/observations.jsonl（當日觀測）也在這底下。它是**累積**的，
#    一天一行、隔日對帳靠它。換機器等於把稽核歷史留在舊機器上——
#    重灌前記得複製出來，那份資料重建不回來。
state/

# 執行紀錄
logs/
*.log

# tools/verify_order_path.py 抓到的原始回報。
# ⚠️ 內含交易帳號——回報字串裡有 CustNo 欄位，未經遮罩。
captured/

# ─────────────────────────────────────────────────────────────
# Python
# ─────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 虛擬環境
.venv*/
venv/
env/

# ─────────────────────────────────────────────────────────────
# 編輯器與作業系統
# ─────────────────────────────────────────────────────────────
.vscode/settings.json
.idea/
*.swp
Thumbs.db
desktop.ini
.DS_Store

# ─────────────────────────────────────────────────────────────
# 刻意「不」忽略的東西 — 這些是專案資產，不要加進來
# ─────────────────────────────────────────────────────────────
#   .scratch/        ticket 追蹤，是有價值的紀錄
#   .env.example     設定範本
#   CONTEXT.md       詞彙表，skill 從根目錄讀取
