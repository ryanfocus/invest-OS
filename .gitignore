# ─────────────────────────────────────────────────────────────
# 機密 — 這裡面是可以直接下單的期貨帳密，外洩等於帳戶被接管
# ─────────────────────────────────────────────────────────────
.env
.env.local
.env.*.local
!.env.example          # 範本必須進版控，別被上面的規則掃到

# 群益下單憑證與金鑰（正常裝在系統憑證區，但以防有人複製進專案）
*.pfx
*.p12
*.cer
*.crt
*.key

# ─────────────────────────────────────────────────────────────
# 執行期產物 — 每台機器各自維護，不同步
# ─────────────────────────────────────────────────────────────
# 部位狀態。這是 13:40 出場的唯一依據，兩台機器共用會災難性地互相覆蓋
#
# ⚠️ state/observations.jsonl（當日觀測）也在這底下。它是**累積**的，
#    一天一行、隔日對帳靠它。換機器等於把稽核歷史留在舊機器上——
#    重灌前記得複製出來，那份資料重建不回來。
state/

# 執行紀錄。**三種來源混在這裡**（2026-08-21 合併）：
#   1. 我們自己寫的     entry-YYYYMMDD.log / exit-YYYYMMDD.log
#   2. 券商回覆的原始存檔 replies-*.txt、Get*Report.txt
#      ⚠️ 內含交易帳號，未經遮罩
#   3. **群益元件自己寫的** Center.log / Reply.log 等
#      ⚠️ 內含使用者的身分證字號。檔名與內容我們都控制不了。
#
# 由 housekeeping.purge_old_logs 於 30 天後自動清除——那不只是省空間，
# 是限制個資留在磁碟上的時間。
logs/
*.log

# ⚠️ captured/ 已於 2026-08-21 併入 logs/。這一條留著是為了舊機器上
#    可能還有殘留的那個資料夾——內含未遮罩的交易帳號。
captured/

# ─────────────────────────────────────────────────────────────
# Python
# ─────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 虛擬環境
.venv*/
venv/
env/

# ─────────────────────────────────────────────────────────────
# 編輯器與作業系統
# ─────────────────────────────────────────────────────────────
.vscode/settings.json
.idea/
*.swp
Thumbs.db
desktop.ini
.DS_Store

# ─────────────────────────────────────────────────────────────
# 刻意「不」忽略的東西 — 這些是專案資產，不要加進來
# ─────────────────────────────────────────────────────────────
#   .scratch/        ticket 追蹤，是有價值的紀錄
#   .env.example     設定範本
#   CONTEXT.md       詞彙表，skill 從根目錄讀取
