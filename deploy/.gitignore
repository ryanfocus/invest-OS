# 建置產物一律不進版控。
#
# 這些目錄裡會出現：打包好的 exe、從版控取出的設定檔、以及（如果檢查沒擋住的話）
# 任何被誤複製進去的東西。它們是「產出」不是「來源」——來源是 templates/ 與
# build.ps1，那兩樣才進版控。
#
# ⚠️ 特別注意 dist/：組裝過程中它會短暫含有交付包的完整內容。
#    build.ps1 結尾有一道檢查，沒過就把它整個刪掉，但別依賴那個——
#    這一行才是最後一道防線。
.build-venv/
build-dist/
build-work/
build-spec/
dist/
