# OS 策略設定
#
# ⚠️ 這裡的每一個 key 都必須有程式讀取（config.KNOWN_KEYS），
#    測試會檢查兩邊一致。加了沒人讀的設定會讓測試失敗——這是刻意的，
#    孤兒設定會讓人以為改了有效，其實沒有。
#
# 帳號密碼不在這裡，在 .env（不進版控）。

discord:
  # Discord 訊號發報。關閉時完全不發出 HTTP 請求。
  enabled: true
