# 08 — 排程與部署

**What to build:** 程式不再需要人工執行。Windows 工作排程器在進場與出場時間各叫醒它一次，
完整跑過一個交易日——早上收到訊號通知，下午正常出場。

**Blocked by:** 06

**Status:** 排程與部署完成（2026-08-23）；剩**雙軌測試**與**完整跑一個真實交易日**

- [x] 工作排程器有兩個工作，分別在進場與出場時間觸發
      —— `tools/setup_schedule.ps1`，每天 08:50 / 13:40。
      **腳本進版控**：在它之前「什麼時候跑」只存在於這台電腦的排程器裡，
      而那是手動設的，換機器就沒了。腳本可重複執行，會先清掉舊的再建，
      所以重跑會收斂到同一個結果而不是越積越多。
      ⚠️ 排程**不管開關**——兩班一律每天叫起來，由程式讀設定檔決定要不要下單。
      把開關編進排程等於多一個「開開關時會忘記」的地方。
- [x] ~~結算日的出場工作提早觸發~~ → **不需要。2026-08-18 定案：結算日不送出場委託。**

      > 這個「排程器不知道哪天是結算日」的設計問題**整個消失了**。
      > 出場排程就是每天 13:40 一班，結算日那班自己判斷後只發一則告知。
      >
      > 原因見 SPEC 使用者故事 20 的修訂：結算日出場失敗其實**無害**
      > （合約到期自動現金結算），一般日出場失敗才是無上限的風險。
      > 我先前把風險方向想反了，於是為結算日設計了一套它不需要的緩衝與重試。
- [x] 已處理電腦睡眠問題：能被喚醒執行，或已確認該機器不會進入睡眠
      **2026-08-16 實測（DESKTOP-DRF3SMO）**：
      - AC 閒置睡眠 = `0`（永不睡眠）；喚醒計時器 = 啟用
      - 沒有閒置自動登出的原則（`InactivityTimeoutSecs` 未設定）
      - `AutoAdminLogon = 1` → **就算 Windows Update 半夜重開，會自動登回桌面**
      - Windows Update 作用中時間原為 `9:00-1:00`，**含 08:44 的排程**；
        已改為 `7:00-1:00`（18 小時是 Windows 上限），把重開窗口壓到 01:00-07:00
- [x] **鎖屏狀態下群益 COM 可正常運作**（2026-08-16 實測）
      排程用「只在使用者登入時執行」——群益登入需要裝在該使用者底下的憑證。
      鎖屏算登入中，實測登入、連報價主機、連回報主機、查帳號、兩個查詢函式
      全部正常。**這原本是無人值守最大的未知數。**
      工具見 `tools/schedule_verification.ps1`。
- [x] 排程執行時的工作目錄與環境正確，程式找得到設定檔與狀態檔
      —— 2026-08-23 實跑驗證：`結果=0`，日誌正確產生，非交易日安靜結束。
      （這是 cron/排程最常見的失敗原因）
- [x] 執行紀錄寫入 log 檔並保留 **30 天**（`housekeeping.purge_old_logs`）
      —— 跟在每天早上那班的最後面，不另開排程（少一個換機器會忘記重建的東西）。

      ⚠️ **清理的目的不只是省空間，是限制個資留在磁碟上的時間。**
      2026-08-21 發現群益的 COM 元件會自己往工作目錄寫日誌，
      而那些檔案**內含使用者的身分證字號**——檔名與內容我們都控制不了。

      `captured/` 已併入 `logs/`（一條規則比兩條安全，漏掉一個的後果是帳號一直留著）。
      ⚠️ 但 `open-price-samples.jsonl` 移到 `state/`——那是**累積型**的樣本紀錄，
      放進會被清掉的資料夾等於毀掉正在累積的證據。
- [ ] 完整跑過一個真實交易日並驗收
      ⚠️ 2026-08-21 **不算**：進場是憑證修好後手動跑的，不是排程叫起來的。
      要的是「排程自己跑完一整天、人事後檢查」。
- [x] 部署與接手文件完成，含「換機器時要重做哪些步驟」→ [docs/DEPLOY.md](../../../docs/DEPLOY.md)
      七個步驟，其中兩個特別容易漏而後果最不對稱：
      **憑證綁電腦要重新申請**（看報價不用、只有下單要，所以會一路正常到送單那一刻）、
      **`state/` 要複製過來**（裡面是重建不回來的稽核歷史）。
- [x] ~~雙軌測試機制~~ → **2026-08-23 決定不做**，理由與完整論證見
      [ticket 02](02-real-broker-login-and-open-price.md)。
      一句話：這個系統的失效**幾乎全部是大聲的**，唯一安靜的那格是後備管道；
      而監控與失敗發生在同一時刻（08:50），提供不了提前量。
      （從 ticket 02 延後而來——需要 COM + 帳號，只有在真機上才有意義）

## 🚦 上線前的硬性關卡

> **絕不允許在沒有實機測過 API 下單的情況下上線。**
>
> 這條不是建議。一個從來沒有真的送出過委託的下單路徑，
> 通過再多假 broker 的測試也只證明「程式照我想的方式運作」，
> 不證明「群益會照我想的方式回應」。

無人值守代表沒有人在旁邊接管。所以下面每一條都必須**已經完成**，
不是「應該沒問題」：

- [x] ticket 04 的**實機里程碑**完成（2026-08-21，1 口微台完整來回）
      ⚠️ 但只驗到同方向持倉那一半，跨越零仍未驗證
- [x] ~~ticket 06 里程碑 2~~ → 已與上一條合併：完整來回跑過一次，出場倉別經實測確認
- [x] 真實 `OnNewData` 原始字串已存成 fixture，且解析有對著它的測試（2026-08-17）
      （不是對著我自己從文件推導的欄位位置）
- [x] `nOpen` 在 08:50 與期交所一致（2026-08-18／19，三來源三商品全合）
- [x] 結算日當天的合約選取經實測確認（2026-08-19，`202608` 未翻月）
- [ ] 上述任一項未完成 → **`auto_enabled` 維持 `false`，只發訊號不下單**

### 只發訊號也是一種上線

如果上面有項目卡住，**不必整個停擺**：把 `auto_enabled` 維持 `false` 就能先上排程。
那樣每天照常收到訊號通知，零金錢風險，而且能持續累積 `nOpen` 對帳資料——
這正是 SPEC 一開始就設計成「開關預設關閉」的原因。

**分兩階段上線是預期中的路徑，不是退而求其次。**
