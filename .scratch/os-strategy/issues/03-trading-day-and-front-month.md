# 03 — 交易日與近月合約判定

**What to build:** 非交易日執行時，程式完全靜默結束——不登入、不發訊息。
交易日執行時，程式知道三個商品的近月合約是哪個月份，也知道今天是不是結算日。

**Blocked by:** 02

**Status:** 大致完成（267 tests passed）；**結算日合約那條的驗證已排程在 2026-08-19（明天）**

- [x] 週末、國定假日執行 → 不登入、不發任何訊息、正常結束
- [x] 交易日執行 → 取得三個商品的近月合約年月
- [x] 合約年月來自商品清單查詢，**不是由日期推算**（避免假日順延造成偏差）
- [x] 同時取得最後交易日，作為結算日的判定依據
- [x] 結算日當天能被正確識別
- [x] 交易日判定沿用 invest-hm 的既有實作，並補上跨年、補班日的測試
- [ ] **結算日當天進場，使用的是即將到期的那個近月合約**
      （該合約當天 13:30 停止交易，這正是出場要提前的原因；
      若誤選次月，出場邏輯與結算日規則會全部對不上）
      ⚠️ **尚未驗證。** 現有測試用的是 2026-08-07 的商品清單快照，距結算日 12 天，
      只能證明「非結算日時近月連續代碼指向本月」，無法證明「結算日當天不會翻到次月」。
      實機驗證也是 08-10，同樣不是結算日。**必須在 2026-08-19 當天實跑一次才能打勾。**

      > 📌 **已排程（2026-08-19）**：`main.py entry` 08:50（`auto_enabled: false`，
      > 只取合約與訊號、不下單）、`tools/compare_open.py` 08:56 與 16:00。
      > 要看的是那天的日誌裡三個商品的近月年月是不是 **`202608`**（本月）而非 `202609`。
      >
      > ⚠️ 這次**驗不到**結算日的出場路徑：開關關著，不會有部位記錄，
      > 13:40 那班會在「沒有今日記錄」就結束。結算日不出場的新分支
      > （2026-08-18 定案）要等下個月才有機會實跑。

## 實作備註

**結算日刻意不沿用 invest-hm 的日期算式。** invest-hm 用「每月第三個週三」
（`weekday==2 and 14 < day < 22`），本專案改用商品清單回傳的**最後交易日**。
測試 `test_settlement_detection_does_not_use_the_third_wednesday_rule` 專門守這件事：
構造一個順延到 08/20 的最後交易日，若實作偷用日期算式就會判錯。

**交易日判定多了臨時覆寫。** `holidays` 套件只知道國定假日，不知道補班日
（週六卻開市）與颱風假（平日卻休市），兩種在台灣都真實發生過，invest-hm 就踩過颱風假。
設定檔的 `calendar.extra_closures` / `extra_openings` 可指定，**兩者同時列出時休市優先**。

**但設定檔不是颱風假的防線，L0 才是。** 公告多半在前一晚或當天清晨才出來，
要求使用者及時更新設定等於把防護建立在人不會忘記上。真正的機制是：
期交所沒開市 → 群益繼續給前一交易日的報價 → `nTradingDay` 對不上今天 → 無法判斷、不下單。
`test_unannounced_closure_produces_no_signal_even_though_the_data_looks_perfect` 守這條。

⚠️ 這條線一度**沒有任何測試守著**：假 broker 收下 `expected_trading_day` 卻不使用，
所以把 `main.py` 的 `trading_day=to_yyyymmdd(today)` 改成 `None`，107 個測試全數通過。
已補 `FakeBroker(quotes=...)`（走真正的 `build_open_prices`）與三條端到端測試，
同一個突變現在會讓 2 個測試變紅。

**非交易日連登入都不做。** 登入失敗會發告警，而 Discord 的沉默只准有一種解釋：
今天休市。測試斷言 `broker.login_calls == 0`。

## 實機驗證（2026-08-10）

```
近月合約 {'TX00AM': '202608', 'MTX00AM': '202608', 'TM0000AM': '202608'}
第 1/3 次取開盤價未就緒：TM0000AM 開盤價為 0.0（尚未成交）
開盤價 大台=44987 小台=45000 微台=45007  訊號=SHORT
```

第一次訂閱後微台資料尚未到達，重試第二次成功——重試機制在生產環境自我驗證。
