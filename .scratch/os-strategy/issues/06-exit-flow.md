# 06 — 出場流程

**What to build:** 到了出場時間，程式讀取當日進場記錄，送出**等量反向**委託平倉。
失敗會自動重試；三次都失敗時發出 Discord 告警，讓使用者能立刻手動處理，
不會在不知情的狀況下讓部位進入夜盤。

**Blocked by:** 05

**Status:** 程式完成；**出場倉別待實機里程碑驗證（與 ticket 04 合併）**

- [x] 出場委託的口數等於狀態檔記錄的**實際成交口數**，方向與進場相反
- [x] 出場委託為市價 IOC
- [x] **出場的倉別（`sNewClose`）填什麼已決定並記錄**
      —— 進場填「新倉」，出場該填「平倉」(1) 還是「自動」(2)？
      這與進場的倉別是**兩個獨立的未知數**，進場驗過不代表出場的填法就對。
      填錯的後果不是報錯，而是**平倉單被當成新倉**，部位不減反增。
- [x] 狀態檔無當日記錄，或當日訊號為不動作 → 什麼都不做
- [x] 出場失敗時每隔一分鐘重試，最多三次
- [x] 三次都失敗 → 發 Discord 告警，訊息內容足以讓使用者判斷該手動平掉什麼（商品、方向、口數）
- [x] **結算日一張單都不送**，並標記狀態檔為「由結算了結」（`close_reason=SETTLEMENT`）
- [x] ~~結算日發一則 Discord 告知~~ → **2026-08-21 拿掉**。使用者定案
      「出場成功不用特別推播」，而結算日什麼都沒錯。落差改記 log 與 `close_reason`。
- [x] **結算日＋不確定**時仍要發——那不是「出場成功」，是真的有事不對勁；
      訊息不得叫人去平倉（合約已停止交易，做不到）
- [x] 結算日的告知要寫明結算價來自**現貨指數**，當日損益會與回測有落差
- [x] ~~結算日的出場**時間**提早~~ → **不需要**，出場排程每天同一班

      > ✅ **2026-08-18 定案：結算日不出場。策略作者的原話——「結算就讓他進結算吧」。**
      >
      > **這條我想錯了兩次，兩次的錯法都值得留著。**
      >
      > **第一版**寫「13:30 觸發 ＋ 不重試」，兩條互相矛盾：期交所規格的最後交易日
      > 交易時間是 **08:45~13:30**，13:30 是**停止交易**的時刻，不是可以下單的時刻。
      > 在 13:30 才觸發，唯一那次嘗試卡在截止線上，而「不重試」讓它沒有第二次機會。
      >
      > **第二版**（2026-08-17）由此推論「結算日才是失敗無法挽回的那天，要提早到 13:25
      > 並開放重試」。**風險方向搞反了**：
      >
      > | | 出場失敗的後果 |
      > |---|---|
      > | 一般日 | 部位進夜盤 15:00–05:00，無停損、無上限、**要人手動處理** |
      > | 結算日 | 合約到期**自動現金結算**，部位一定會平掉，**不用做任何事** |
      >
      > 結算日反而是唯一會自己收尾的一天。所以正確做法不是「提早一點送」，是**不送**。
      > 代價（出場價變成現貨指數的結算價）已知且有界，一個月最多一次。
      >
      > **順帶修掉一個真的缺陷**：舊行為在結算日出場失敗會發出
      > 「🚨 請立刻手動送出：賣出 N 口 MTX08」——合約已經停止交易，那件事**做不到**，
      > 照著做只會在別的月份開新倉。現在改成一則平靜的告知。
      >
      > ⚠️ 「進結算」**只講出場**。結算日照常在 08:50 進場。
- [x] 出場成功後更新狀態檔
- [x] 例行出場**不發** Discord，只有失敗才發
- [x] 時間由參數傳入而非讀系統時鐘，測試可直接指定結算日與一般交易日
- [x] **前兩次出場失敗、第三次成功 → 正常完成，不發告警**
      （證明重試真的會再試；不斷言間隔或呼叫次數，那是實作細節）

## 從 ticket 05 接手的部分

05 建好了「不確定」這個狀態與它的契約（`PositionRecord.is_uncertain`、
`lots` 在不確定時為 `None`），但**執行端在出場流程，而出場流程是這張**。
所以下面這幾條由 06 負責，05 那邊已標為待本張完成：

- [x] 狀態為「不確定」時（`record.is_uncertain`）→ **不送出任何委託**，
      改發 Discord 要求人工處理
      —— 不知道持有幾口就下單，可能開出反向新倉，比什麼都不做更糟

      > 📌 [ticket 09](09-fill-query-fallback.md) 會在這一步之前插入一次
      > `GetFulfillReport` 查詢：查到了就自動復原、照常平倉，查不到才走這條。
      > **本張先做「查不到」這條**——它是最終防線，09 只是讓它更少被觸發。
- [x] 出場後比對「應平口數」與「實際成交口數」，有殘留 → 發 Discord 告警，
      訊息含商品、方向、**殘留幾口**，足以讓使用者直接手動處理
- [x] **部分成交不視為出場成功**，狀態檔不可標記為已出場

⚠️ **`record.lots` 的型別是 `int | None`。** 上面第一條驗收條件就是 `None` 的處理；
先檢查 `is_uncertain` 再用 `lots`，直接拿去當口數會把 `None` 送進委託。

## 🔴 實機里程碑（與 ticket 04 合併）

**執行程序見 [ticket 04 的「實機里程碑：第一次完整來回」](04-entry-order-and-position-state.md)。**

> **2026-08-20 合併。** 原本本張有獨立的「里程碑 2」，前置條件是「ticket 04 的
> 里程碑 1 已完成」。分開的理由是里程碑 1 定義時**出場流程還不存在**——
> 那個理由已經沒了，而分開反而更貴也更危險：
>
> - 兩天真錢，而完整來回那天的曝險本來就躲不掉
> - 里程碑 1 寫的「進完手動平掉」現在會讓 13:40 那班把手動平倉變成**反向新倉**
>   （實測重現過）

本張要從那次實機取得的事實（打勾請一併更新 04 的同一條）：

- [x] **出場倉別**（`sNewClose=2`）的實測行為 → `tests/test_order_fields.py`
      **2026-08-21**：帳上 2 口多單時賣 1 口，成交（序號 2315609905580 @ 45135），
      帳戶回到 1 口。成交查詢的倉別欄位回 `O`，與進場那筆的 `N` 不同——
      **券商沒有把它當成新倉**。
      ⚠️ **但那次沒有跨越零**，而跨越零正是選「自動」的唯一理由。核心論證仍未實測。
- [ ] **平倉的成交回報原始字串**（`OnNewData` 推播）→ **機制已就緒，等下次真單**

      ✅ **2026-08-23 做好了那個「正確的做法」**：`place_order` 現在會把
      整段視窗收到的原始回覆存進 `logs/replies-<時間>-<序號>.txt`，
      成功、失敗、不確定都存——**收不到回報那天最需要它**。

      存**整個視窗**而不只自己那筆：2026-08-19 就是靠「別人的單也在同一條線上」
      這個證據，才發現序號比對的漏洞。

      ⚠️ 存檔跑在 `finally` 裡，而 `finally` 拋出的例外會**取代**正在傳遞的那個。
      所以它包了一層 try——否則使用者收到的會是「寫檔失敗」而不是
      「收不到成交回報」，真正該處理的問題被無聲蓋掉。有測試守著這一條。

      2026-08-21 那次**刻意放棄**用第二個 API 連線去抓：互踢的最壞後果是
      出場的回報連線建不起來 → 委託送不出去 → 部位過夜。
      用「可能拿不到一份 fixture」換「可能部位過夜」，不划算。
- [x] **平倉的成交查詢原始列** → 已存成 fixture（見上）
- [x] **完整一天的狀態檔內容** → 2026-08-21 的真實內容：

      ```json
      {"trading_day": 20260821, "product": "TM0000AM", "order_code": "TM2609",
       "contract_month": "202609", "side": "BUY", "lots": 1, "requested_lots": 1,
       "last_trading_day": 20260916, "status": "CONFIRMED",
       "order_seq": "2315609807637", "exited": true, "close_reason": "EXIT",
       "uncertain_stage": ""}
      ```

⚠️ 「**平倉後仍有殘留**」那組驗收條件**這次跑不到**——1 口的單不會有部分成交。
那條只能靠假 broker 測，或等日後真的遇到。不要因為這次順利就把它當成驗過了。

## 實作備註

**出場倉別選「自動」(2)。⚠️ 這是兩個都有缺點的選項之間的判斷，不是已證明的結論。**

初版把它寫得像已經證明了，code-review 戳破：**同一個跨越零的問題在進場也成立**
（08:50 賣 3 口時帳上是 +1），但進場用的是「新倉」。兩邊不一致，而我只改了出場。

| | 好處 | 代價 |
|---|------|------|
| 平倉 (1) | 部位不在時**被拒**，失敗得大聲 | 語意是「平掉 N 口」，N 大於現有部位時做不到 |
| **自動 (2)** | 能跨越零 | 部位不在時**安靜地開出反向新倉**——正是本張開頭警告的失效模式 |

真正的答案只能靠實機里程碑（ticket 04）。目前選「自動」，但**論證有洞，別當成定案**。

**出場刻意不查商品清單。** 下單代碼與最後交易日在 08:50 就寫進狀態檔了
（`last_trading_day` 是本張新增的欄位）。13:40 再連一次報價主機只是多一個
會失敗的地方，而那時候失敗的代價是部位過夜。

**結算日的判斷排在「不確定」與「開關關閉」的前面。** 那兩條的作用是**阻止送單**，
而結算日本來就不送——走到那邊只會發出一則叫使用者去做做不到的事的告警。
不確定的情況改由結算告知那則一併講清楚（部位照樣被結算，但成交口數仍然未知，
要自行對帳）。

⚠️ 這個欄位也踩到同一類的洞：加了欄位、出場測試全綠，但**進場根本沒把值寫進去**，
於是結算日永遠判不出來、照常重試。24 條出場測試都沒抓到——因為它們用的是手工組的
記錄。補上端到端測試（進場寫 → 出場讀）才擋得住。這是第三次了。

**兩班改成獨立的進入點**：`python main.py entry` / `python main.py exit`。

## code-review 抓到的東西（2026-08-16）

| # | 問題 | 後果 |
|---|------|------|
| 1 | 出場的重試迴圈把 `FillUnknown` 跟 `OrderFailed` 一起攔下來重試 | **第一筆若其實已經平掉，第二筆就是開出反向新倉**——而出場倉別是「自動」，券商不會擋 |
| 2 | 出場收不到回報時完全沒寫狀態檔 | 記錄停在「有 N 口、還沒出場」，那是個看起來很確定的錯誤 |
| 3 | 開關關著但帳上有記錄時靜默結束 | 部位過夜而沒有人知道 |
| 4 | 狀態檔壞掉時共用「今日無訊號」那則 | 13:40 出現那句話只會讓人以為今天沒事 |
| 5 | 口數斷言寫成 `"2" in text` | **日期 2026/08/10 本身就含 0、1、2，那條斷言不管口數多少都會過** |
| 6 | 斷言重試次數（`len(orders) == 1`） | SPEC 只認可「零呼叫」斷言；改成用**結果**分辨有沒有重試 |

第 1 項違反的是我自己寫在 `broker/__init__.py` 的規則：
「`FillUnknown` → 可能已經成交 → 絕不可以自動送單」。

順帶把散在三處的「方向反轉」收成一個家（`PositionRecord.exit_side`）——
那個對應寫反的後果是部位加倍，散開遲早不一致。`intent` 也改成必填：
給預設值的話，出場少傳一個參數就靜默變成新倉。

## 留給後續的兩個問題

1. **部分成交後不重試剩餘量。** 目前只記錄殘留並告警。要不要自動再送一次剩下的口數？
   ticket 沒要求，而重試與部分成交交互的邏輯很微妙（重試時該送幾口？）。
   但「還剩 1 口會過夜」正是本系統要防的事——值得討論。
2. **出場重試沿用 `quote.retry_interval_seconds`。** 名字是報價的，語意上不對。
   要嘛改名成通用的重試設定，要嘛給出場自己的設定項。
