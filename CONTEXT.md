# CONTEXT — invest-os 詞彙表

> 這份文件**只放詞彙定義**，不放實作細節、不放規格、不當草稿紙。
> 規格看 [docs/SPEC.md](docs/SPEC.md)，架構決策看 [docs/adr/](docs/adr/)。

---

## OS

**本專案中，「OS」一律指這個策略的名稱**，由策略作者命名。

⚠️ 與群益 API 的 `OS` **無關**。群益用 `OS` 表示「複委託／海外證券」下單模組
（`TF`＝台灣期貨、`TS`＝台灣證券、`OF`＝海外期貨、`OS`＝複委託）。
本策略交易的是**台灣期貨**，用的是群益的 **`TF`** 模組。

看到 `OS` 時請依上下文分辨：專案名／策略名 → 本策略；群益 API 文件 → 複委託。

## 開盤價

**當日 AM 盤（日盤）08:45 集合競價產生的第一個成交價**，非即時價、非前一日收盤價。

三個商品（大台、小台、微台）各有自己的開盤價，策略只比較這三個數字。
盤中價格變化與本策略無關。

⚠️ **必須是 AM 盤，不是全盤。** 台指期有夜盤，兩種盤別的「開盤價」是不同的數字：

| 盤別 | 涵蓋時間 | 「開盤價」是什麼 |
|------|---------|----------------|
| **AM 盤（本策略要的）** | 當日 08:45–13:45 | **08:45 的第一個成交價** ✅ |
| 全盤 | 前一日 17:25 → 隔日 05:00，再加當日 08:45–16:15 | 前一日 **17:25 夜盤**的開盤價 ❌ |

（群益 K 線文件原文：「國內期選—全盤: EX: 指定日期區間為 2023/9/5 全盤 K LINE，
將包含 2023/9/4 17:25 開盤後~2023/9/5 05:00，及 2023/9/5 8:45~16:15」）

已驗證：策略作者提供的看盤畫面 `開 42331`，與期交所「一般時段」CSV 的開盤價
（2026/07/31 大台 42331）完全一致 → 策略要的確定是 AM 盤。

事後對帳來源：期交所 `dlFutDataDown` CSV 第 `[3]` 欄（該檔的「交易時段」欄位為「一般」）。

## 大台 / 小台 / 微台

| 中文 | 期交所商品代號 | **群益報價代碼（近月、日盤）** | 群益下單後台代碼 |
|------|--------------|---------------------------|----------------|
| 大台（臺股期貨） | `TX` | **`TX00AM`** | `FITX` |
| 小台（小型臺指期貨） | `MTX` | **`MTX00AM`** | `FIMTX` |
| 微台（微型臺指期貨） | `TMF` | **`TM0000AM`** | `FITMF`（未證實） |

⚠️ **報價代碼一定要帶 `AM` 後綴。** 不帶後綴的 `TX00` / `MTX00` / `TM0000` 是**全盤**
（含夜盤），開盤價會是夜盤的開盤，不是策略要的 08:45 那筆。
2026-08-07 實測驗證，詳見 [ADR-0005](docs/adr/0005-use-am-session-quote-codes.md)。

微台用**四個零**，與大台小台的兩個零不同格式。

契約規模關係（群益文件）：**1 大口 = 4 小口 = 20 微口；1 小口 = 5 微口。**

## 訊號方向

策略每個交易日產生三種結果之一：

| 值 | 條件 | 意義 |
|----|------|------|
| 做多 | 大台開盤價 **>** 小台 **且** **>** 微台 | 買進 |
| 做空 | 大台開盤價 **<** 小台 **且** **<** 微台 | 賣出 |
| 不動作 | 其他 | 當日不進場 |

⚠️ **「相等」只看大台與另兩者的比較，不看小台與微台之間。**

| 情況 | 訊號 | 實測頻率（126 個交易日） |
|------|------|----------------------|
| 大台 == 小台，或 大台 == 微台 | **不動作**（不成立純大於／純小於） | 4 天（3.2%） |
| **小台 == 微台**，大台高於或低於兩者 | **照常產生做多／做空** | **20 天（15.9%）** |
| 三者全等 | 不動作 | 0 天 |

第二列容易被誤讀成「有相等就不動作」。實際規則是策略作者給的公式：
`大台 > 小台 and 大台 > 微台` → 做多。小台與微台彼此是否相等與此無關。

決策依據見 [ADR-0004](docs/adr/0004-ties-between-mtx-and-tmf-still-produce-a-signal.md)
—— **這是刻意的，不是漏判**，改動前務必先讀。

「不動作」是**明確的第三種結果**，不是「失敗」或「無資料」。
資料抓不到是另一回事，不可混為一談——那叫「無法判斷」，見下。

## 無法判斷

**三個開盤價沒能全部取得，因此連比大小都做不到。**

與「不動作」的差別在於：不動作是**判斷出來的結果**，無法判斷是**根本沒判斷成功**。
兩者長得像的話，故障就會被當成正常的一天而沒人追。

| | 意義 | 程式表示 | 結束狀態 |
|---|------|---------|---------|
| 不動作 | 比過了，大台夾在中間 | `NO_TRADE` | 正常 |
| **無法判斷** | 資料未就緒或缺件 | 訊號為 `None` | 正常（拿不到資料是預期內的） |
| 致命 | 登入失敗、環境壞掉 | 訊號為 `None` | **錯誤** |

Discord 訊息也刻意區分：不動作照常發三個開盤價與方向，無法判斷發「⚠️ 今日無訊號」
並附上原因（哪個商品、什麼問題）。

## 固定持有部位

使用者在 OS 之外、自行長期持有的台指期部位。

OS **不管理**它，但因台灣期貨同一帳戶同商品同月份採**淨額**計算，
OS 的部位與它會在帳戶層級互相沖銷。詳見 [SPEC](docs/SPEC.md) 的「部位隔離」
與「使用者原有部位會受到的影響」。

## 進場 / 出場

- **進場**：08:50 依訊號方向送出新倉委託
- **出場**：13:40（結算日 13:30）送出**與進場等量的反向**委託

「出場」永遠指平掉 **OS 自己開的口數**，不指帳戶淨部位歸零。

## 不確定（部位）

**委託已經送出，但系統不知道成交了幾口。** 回報沒回來，或認不出是哪一筆。

這與「無法判斷」是不同層次的東西：無法判斷講的是**訊號**（沒算出來），
不確定講的是**部位**（不知道帳上有什麼）。兩者源自同一個原則——
分不清楚的時候不要猜。

三者必須分得開：

| | 意義 |
|---|------|
| 沒有部位 | 沒進場，或**確定** 0 口成交 |
| 已確認 | 收到成交回報，知道實際成交幾口 |
| **不確定** | 送了單，但不知道成交幾口 |

⚠️ **「不確定」不是「0 口」。** 0 的意思是「確定沒成交」，而任何
「口數大於 0 才處理」的判斷都會靜靜跳過 0——跳過的正是最需要有人來看的那一筆。

不知道持有幾口卻自動送單，可能是加倉、也可能開出一個沒人管的反向新倉，
**比什麼都不做更糟**。

各流程遇到「不確定」時的行為見 [SPEC](docs/SPEC.md)。
